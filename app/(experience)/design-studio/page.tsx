import { DesignStudioMindMap } from "@/components/design-studio/DesignStudioMindMap";

export const metadata = {
  title: "Design Studio — Giantverse",
  description: "Brainstorm every visual parameter of your character: face, hair, wardrobe, materials, world research and artist references.",
};

export default function DesignStudioPage() {
  return <DesignStudioMindMap />;
}
