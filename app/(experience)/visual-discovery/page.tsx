import { VisualCharacterDiscovery } from "@/components/visual/VisualCharacterDiscovery";

export const metadata = {
  title: "Visual Character Discovery — Giantverse",
  description: "Discover which legendary character design language you visually resemble.",
};

export default function VisualDiscoveryPage() {
  return <VisualCharacterDiscovery />;
}
